/** Re-export so imports from `./components/DealEsignTemplatesTab` resolve (canonical module lives under `e_sign_templates/`). */
export {
  DealEsignTemplatesTab,
  ESIGN_ENTITY_CATEGORIES,
  ESIGN_FOLDER_SLUG,
  type EsignEntityCategory,
} from "../e_sign_templates/DealEsignTemplatesTab"
